apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://localhost:9090
    isDefault: true
    uid: prometheus_ds_1

  - name: Pyroscope
    type: grafana-pyroscope-datasource
    access: proxy
    url: http://localhost:4040
